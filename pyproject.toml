[project]
name = "covid"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "FelipePassarela", email = "126916744+FelipePassarela@users.noreply.github.com" }
]
requires-python = "==3.13.*"
dependencies = [
    "hydra-core>=1.3.4",
    "imbalanced-learn>=0.14.2",
    "ipykernel>=7.3.0",
    "joblib>=1.5.3",
    "loguru>=0.7.3",
    "matplotlib>=3.11.0",
    "missingno>=0.5.2",
    "numpy>=2.4.6",
    "omegaconf>=2.3.1",
    "pandas>=3.0.3",
    "pandas-stubs~=3.0.3",
    "pydantic>=2.13.4",
    "pyrefly>=0.60.0",
    "pyyml>=0.0.2",
    "ruff>=0.15.22",
    "scikit-learn>=1.9.0",
    "scipy>=1.17.1",
    "scipy-stubs~=1.18.0",
    "seaborn>=0.13.2",
    "shap>=0.52.0",
    "statsmodels>=0.14.6",
    "tqdm>=4.68.3",
    "typer>=0.26.7",
    "umap-learn>=0.5.12",
    "wandb>=0.28.1",
    "xgboost>=3.3.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=9.1.0",
]

[project.scripts]
split = "covid.ingest.split_data:main"
tune = "covid.tune.cli:main"
ntune = "covid.tune.nested.cli:main"
train = "covid.train.cli:main"
eval = "covid.eval.cli:main"
explain = "covid.explain.cli:main"
