defaults:
  - pipeline: dummy
  - _self_

name: ${hydra:runtime.choices.pipeline}

train_data_path: data/raw/longa_nao_vacinados.csv
output_path: models/${hydra:runtime.choices.pipeline}.joblib

tune_threshold: false
tuning_scoring: balanced_accuracy
